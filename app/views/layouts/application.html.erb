<!DOCTYPE html>
<html>
<head>
  <title>DASTConsulting</title>
  <!--[if lt IE 9]>
  	<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"
  	type="text/javascript"></script>
  	<!end-->
  <script type="text/javascript" src="http://maps.googleapis.com/maps/api/js?sensor=false"></script>
  <%= include_gon %>
  <%= include_gon(watch: true) %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%#= javascript_include_tag 'jquery', 'application' %>
  <%= javascript_include_tag "application" %>
  <%#= javascript_include_tag :locations %>
  <%= csrf_meta_tags %>
  <%= yield :head %> 
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
	<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/houses">DAST Consulting</a>
        <div class="nav-collapse">
          <ul class="nav">
            <li><%= link_to "Houses", "/locations" %></li>

            <%if current_user%>
              <%if request.url.split("/").last=="locations" || request.url.split("/").last=="locations"%>
              <!--<li class="search_header">-->
                  <!--<%#= form_tag("/locations",:class => "navbar-search pull-left header_form", :method => "get", :id => "locations_search") do %>-->
                    <!--<%#= label_tag(:search, "") %>-->
                    <!--<%#= text_field_tag(:search) %>-->
                    <!--<%#= submit_tag("Search", :class => 'btn btn-primary', :id => 'search_but_header') %>-->
                  <%# end %>
                  <%else%>
                  <li class="search_header">
                  <%= form_tag("/houses",:remote=>"true",:class => "navbar-search pull-left header_form", :method => "get", :id => "map_locations_search") do %>
                    <%= label_tag(:search, "") %>
                    <%= text_field_tag(:search) %>
                    <%= submit_tag("Search", :class => 'btn btn-primary', :id => 'search_but_header') %>
                  <% end %>
                  <%end%>
                <li class="search_header">
            	<li ><%= link_to 'Sign out', destroy_user_session_path, method: :delete%>
            <%else%>
            	<li ><%= link_to 'Sign in', new_user_session_path%>
            <%end%>	
          </ul>
        </div>
      </div>
    </div>
  </div>

<div class="container">
	<div class="row">
    <div class="span12"><%= yield %></div>
</div>
  </div>  
</div>  

<%= yield :scripts %>
</body>  
</html>
